# TODO

- [x] Understand how TanStack Start bootstraps HTML in this repo (existing entry points).
- [x] Add a minimal, safe `index.html` compatible with Vercel deployment.
- [x] Ensure routing works for SPA-style deep links (via Vercel config rewrite).
- [x] Build locally (`npm run build`) to verify.
- [x] Add `vercel.json` rewrite rules for `/*` -> app.
- [x] Provide final summary of changes.



